#include<stdio.h>
#define s 10

int main()
{
    int arr[s]={2,3,6,3,4,6,7,1,10,12};
    int position=2;
    for(int i=position;i<s-1;i++)
    {
        arr[i]=arr[i+1];
    }
    arr[s-1]=0;
    for(int i=0;i<s;i++)
    {
        printf("%d ",arr[i]);
    }
    
    return 0;
}
